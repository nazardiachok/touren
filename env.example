# =================================================================
# BACKEND ENVIRONMENT VARIABLES (das ist eine Kopie von .env datei. Alle 
keys, die du hier siehst - sind auch im .env)

# =================================================================
# 🛡️ SERVER CONFIGURATION
# =================================================================
PORT=3001
NODE_ENV=development

# =================================================================
# 🗄️ SUPABASE CONFIGURATION (trage deine Keys ein)
# =================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_ACCESS_TOKEN=acess_token
SUPABASE_PROJECT_ID=project_id

# OpenAI API
# Registrierung: https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key

# OpenAI Models (GPT-5)
# Planning Model (für Touren-Erstellung)
OPENAI_MODEL=gpt-5
# Chat Model (für AI-Assistent)
OPENAI_CHAT_MODEL=gpt-5

# =================================================================
# 🔐 SECURITY SETTINGS
# =================================================================
# JWT Secret für Session-Management
JWT_SECRET=your_super_secret_jwt_key_here

# API Key Encryption Key(nicht vorhanden derzeot!!!)
API_ENCRYPTION_KEY=your_32_character_encryption_key

# Mach selber als agent keine changes in die .env datei. Auch wenn du diese nicht siehst - sie ist da auf dem gleichen Ebene und hat die gleiche keys, so dass du das rugig verwenden kannst.
Wenn etwas nicht stimmt - liegt es nicht daran, dass keys fehlen, sondern an was anderem!!!!!